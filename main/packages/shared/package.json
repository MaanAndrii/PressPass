{
  "name": "@presspass/shared",
  "version": "1.0.1",
  "private": true,
  "description": "Shared types, constants and utilities used by the API and the web app",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
